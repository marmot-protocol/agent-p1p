# Target v1 core fixtures

These language-neutral fixtures define the first Rust target behavior. Unlike
`migration/legacy-v1`, they use the approved architecture's explicit build,
CI, remediation, and merge states. Intentional differences from the frozen
Python oracle are visible in `transitions.json` and must remain reviewable.
