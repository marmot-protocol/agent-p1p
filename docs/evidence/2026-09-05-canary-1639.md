# Supervised MDK 1639 shadow canary

**Current state: paused at 10:34 UTC on 2026-09-05 after planner failure.**
The follow-up [failure investigation](2026-09-05-planner-failure-investigation.md)
identifies the provider rejection, retry mechanism, incomplete worker inputs,
and scratch-storage gap; proposed fixes are not yet implemented.
Execution timers and worker gateway are disabled and stopped; inert revision 3
is restored. Ledger history and the issue label remain unchanged. Do not
reactivate or relabel automatically. The supervision heartbeat is paused.

JG explicitly approved [MDK #1639](https://github.com/marmot-protocol/mdk/issues/1639)
and a supervised run. Activation passed at 10:16 UTC on 2026-09-05; trusted
actor `202880` then applied `pip-ok`. No merge is authorized.

## Deployed configuration

- Source: `ab16528588acc1b50e1a30fdb303c6a025bc4a4a`; ledger schema 8.
- Release: `65f5b791c8ec7ebff5237a00ab4bdbe3e6232844a8758536971537c0e474aa6e`.
- Host policy revision 5 changes only revision, intake enable/pause and dispatch
  from source revision 3. Exact models, one-case limits, shadow merge and
  autonomous merge false remain unchanged.
- Active policy SHA:
  `70b70c11f876f4be116dd066d12435256d1a20d3f51239ba8466a94abc4a5114`.
- Inert policy SHA:
  `9750f3bd6d3d3cd0216ae0ab19858719de60f371d694899e49169a1231c34196`.

Pirate staging: `/home/jeff/.cache/pip-canary/1639-ab16528/` contains
`activate-runtime`, `mdk-active.json`, and guarded `pause-runtime`. Invoke the
emergency helper with `bash`; it is staged, not exercised on this live canary.
It stops execution services and restores inert policy while preserving cases,
tasks, workspaces and results. Ingress and conversational Hermes are untouched.
Resume requires inspection, not replaying the pre-label activation script.
Rollback copy: `/etc/pip/repositories/mdk.json.inert-r3-1639` (root-owned).

The initial activation stopped at `sudo -v`, which requested a password despite
temporary NOPASSWD. `sudo -n true` verified access and replaced that probe;
no policy changed on the failed attempt. Successful report:
`activation-20260905T101534Z.log` in the staging directory.

## Verified boundaries and first claim

Before labeling: no other open `pip-ok` issue; no pending spool or active case;
exact direct model availability and worker auth inventory checked; required CI
rule retained; no Hermes default assignee; no-checkout repository shape valid;
mounted workspace with 3.73 TB free; zero-candidate and empty-queue probes passed.
Every execution timer fired twice successfully and had a finite next run.

At 10:17 UTC:

- `repo:1055628515#1639@3` was `PLANNING`, state revision 1, policy revision 5.
- Exactly one new dispatch batch, create reservation and task projection.
- Planner `t_32fd1c63` running since 10:16:54 UTC, assigned `planner`, exact
  `openai-codex/gpt-5.6-sol`, skills commit `ab16528`.
- Ordinary parentless assigned task; zero released synthetic gates.
- Workspace `/var/lib/pip/worktrees/mdk/repo-1055628515-issue-1639-workflow-3`.
- No accepted worker result, direct attempt or PR yet.
- Old `repo:1055628515#891@3` remained ABANDONED at revision 2; historical
  `t_f4473714` remained unassigned/ready with no session or result.

The issue-label webhook created the case. Subsequent polling reported already
owned and active limits reached: expected deduplication, not intake failure.
Controller cycles remained successful; merge disabled.

## Supervision and remaining proof

Task heartbeat `supervise-pip-mdk-1639-canary` checks every five minutes, reports
milestones/failures, and stays quiet on unchanged healthy state. Stop on
unexpected work or authorization/isolation drift and preserve evidence. No
model replacement, Hermes fork, automatic retry/relabel or merge. Pause the
monitor when completed or safely stopped awaiting direction.

This is not an end-to-end success claim. Remaining: planner acceptance and plan
publication, builder/draft PR, independent required reviews plus detached
comparison, exact-head CI, remediation if needed, and final shadow recommendation.

## Planner failure and emergency pause

At 10:30:03 UTC the planner's OpenAI API request was rejected for possible
cybersecurity risk. Hermes logged a non-retryable client error at 10:30:04,
then its dispatcher reaped the worker and spawned another attempt at 10:30:11.
The 10:33 supervision check detected that failure; execution was stopped at
10:34. No model was changed and no retry was initiated by the supervisor.

Earlier tool errors included unauthenticated `gh`, read-only fetch and target
directory writes, missing `jq`, terminal approval denials and unavailable LSPs.
These are separate runtime-contract/prerequisite observations, not proven
causes of the provider rejection. The first attempt had been reading simulator
source; no valid planner result was accepted. Do not weaken safety controls or
change providers to bypass the rejection.

Root-only evidence copies are in Pirate staging `failure-1034/`:
`planner-task.log` and `planner-errors.log`. Original task/profile logs remain.
No credentials were copied into the repository.

The guarded pause helper stopped execution and restored the correct inert
policy hash. Its final inactive-state assertion returned nonzero because the
worker gateway exited with failure during shutdown. Read-only follow-up proved
all three timers disabled/inactive, all three execution oneshots inactive, and
the gateway disabled/failed (not running). Preserve that diagnostic state.
Conversational Hermes and webhook ingress remained active.

Ledger still contains the new PLANNING case at revision 1 and the old abandoned
case unchanged; one dispatch/projection, zero accepted runs, zero direct
attempts, no PR. Queue status alone must not be interpreted as live execution
while services are stopped. Resume needs investigation and explicit direction.
