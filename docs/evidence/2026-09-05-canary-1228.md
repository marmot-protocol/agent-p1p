# Fresh MDK 1228 canary: preparation and pre-label compatibility failure

**Update 2026-09-06 06:22 UTC:** planner completed and its plan was accepted and
published. Execution and supervision are now paused after the direct builder
failed to start. See the continuation below; no automatic retry is authorized.

The subsequent offline-tested repair is documented in
[the workspace handoff evidence](2026-09-06-workspace-handoff.md). It does not
imply that this case has been reset or resumed.

JG explicitly authorized retiring #1639, selecting another issue, and running a
supervised canary. Selected [MDK #1228](https://github.com/marmot-protocol/mdk/issues/1228),
"Hermes ignores Marmot group subjects in chat metadata". This is an independent
display-metadata task, not a replay or reformulation of the rejected request.

## Previous attempt retired

Removed `pip-ok` from #1639. A subsequent live read confirmed removal and no
other open labeled issue. GitHub's initial issue-list read lagged the write;
no reconciliation ran until the empty result was confirmed.

With all worker services/timers stopped, ran the normal controller once using
the exact original revision-5 policy so authoritative revocation matched the
case's frozen policy. The existing active-authorization reconciler recorded
ABANDONED at state revision 2. Restored inert revision 6 in an exit guard.
No inference, new intake, task reset or history rewrite occurred.

Archived only `t_32fd1c63` through stock Hermes CLI. Its active run was closed as
reclaimed by Hermes's normal archive path. Inspected upstream cleanup first:
this task has `workspace_kind=dir`, which archive deliberately preserves. Its
worktree, profile caches, logs, original Sol binding and historical results are
retained. Old abandoned #891 and its unassigned gate remain unchanged.

Root-only snapshots of the ledger and actual `pip-mdk` board database are in
`/home/jeff/.cache/pip-canary/1228-6b0a144/pre-retirement/` on Pirate. Retirement
returned `PIP_1639_REVOKED_AND_ARCHIVED_WITH_EVIDENCE_RETAINED`.

## Candidate evidence and controls

#1228 is open, unassigned, and has named/unnamed/error/subject-refresh acceptance
criteria. Current master `8395b97bbd75c158be8d2ae293c56c5c1211d443` still has
`get_chat_info()` returning only a hex-derived display name at adapter lines
2007-2013. No overlapping open PR was found. This is source verification, not
the planner's reproduction or an assertion that issue comments are current.

Cursor's fresh model catalogue contains all three exact configured direct
models. Safe Master still requires `Required CI` from integration 15368.

Staged active revision 7 changes only revision, intake enable/pause and dispatch
from the installed revision-6 policy. Models, one-case concurrency, one Hermes
attempt, scratch layout, and shadow/non-autonomous merge remain unchanged.

- Installed source: `6b0a1444b9b38c85a5acc5a7d618a9dfe04629f1`.
- Active policy SHA: `7125de44f82b4ecd6454198aff44ad9a2f426755678235cd1961d77fdbca23ec`.
- Inert policy SHA: `f6868eafe0262208e3afa6fe17d566972ba8250342b7f2dfb2aab296a81ffe7f`.
- Staging: `/home/jeff/.cache/pip-canary/1228-6b0a144/`.

## Activation stopped before labeling

Activation at 20:47 UTC drained 17 webhook deliveries under inert revision 6,
confirmed no active cases and only the expected archived/unassigned cards, then
reconciled revision-7 profiles. Its no-candidate controller probe failed with
`stored worker projection has an invalid immutable binding` before any gateway
or execution timer was enabled. The exit guard restored inert revision 6 and
profiles. No #1228 label, case, provider request, branch, or PR was created.
Conversational Hermes and webhook ingress were left running.

The result reader validates all unconsumed projections against current role
models *before* checking terminal/superseded case state. Thus the archived Sol
projection fails validation against Astra even though it cannot produce an
accepted result. Historical evidence must stay immutable, not be rebound or
deleted to make the controller pass.

## Surgical regression fix

Expanded the late-result test to include policy/model migration after abandoned,
taken-over, superseded, completed, blocked and escalated states. It failed with
the same `InvalidProjection`. Moved current-policy validation after the existing
case-state/revision eligibility check. Current work still receives all original
binding checks before any Hermes query; a separate model-drift regression proves
it fails without a query or mutation. Focused result-cycle tests pass.

Local release gates passed: 305 workspace tests, zero failures, five explicitly
external probes ignored; all-target/all-feature Clippy with warnings denied;
formatting, whitespace, supply-chain pins/regressions, Node 24 action pins and
release-workflow contract checks. This does not replace the pending signed
release or another live controller preflight.

The corrected binary still needs release verification and deployment before
activation can resume. The supervision heartbeat was retargeted to #1228, then
paused on this pre-label failure. Do not run the staged activation unchanged
after installing a different release: refresh its exact source/release guards.

## 2026-09-06 corrected installation and first claim

JG approved run 33991359567. Verified and installed source
`21dc6da1f7643c27f80d5615bebf5ed096512232`, manifest
`f3ea20e81a0ea237bc7ee070d28f1253f1a3195877edb6a64d6eae5f2dc7c89b`, binary
`4c42505c9a7e507aa1488ac982b639814b2adf40cba5a657618eabad6ec7b3ed`.
The local trusted verifier, prior installed verifier and root-staged installer
verified the cohort. Paused installation/bootstrap preserved logical dumps of
the ledger and actual `pip-mdk` board database and the auth-file hash. Snapshots
and logs are retained under the source/run directory in Pirate's
`/home/jeff/.cache/pip-deployments/`.

The refreshed `activate-runtime-21dc6da` script passed on 2026-09-06 at 06:06 UTC:
no active cases before labeling, no unexpected board work, all three execution
timers fired twice and had finite next runs. Policy revision 7 and its hash
remain the same as staged above. Emergency pause restores revision 6; its
policy guard remains valid. Old activation scripts must not be reused.

Applied `pip-ok` to #1228 only after these gates. Fresh live evidence:

- case `repo:1055628515#1228@3`, PLANNING revision 1, policy revision 7;
- task `t_94fd9479`, running as planner, exact `gpt-6-astra`, one attempt;
- frozen skills/source commit `21dc6da1f7643c27f80d5615bebf5ed096512232`;
- frozen issue-context object present in the authorization evidence bundle;
- private managed scratch at
  `/var/lib/pip/worktrees/hermes-scratch/6b951273c4e3cf9c333aaf05fe24131140b5712cfe6c875bf744a7a6643bd25a`;
- exactly one new dispatch batch, reservation and task projection;
- repeated controller cycles successful, no accepted result or PR yet;
- merge disabled; historical #891/#1639 state and bindings preserved.

The five-minute supervision heartbeat is active for #1228, with failure-stop
instructions and no automatic retries, model substitution or merge. Temporary
passwordless sudo expires at 07:35:58 UTC; the monitor is instructed to pause
by its first check at/after 07:25 UTC unless authorization is explicitly
extended. This is app-based supervision, not a guaranteed host-local cutoff.
Conversational Hermes and webhook ingress remain independent.

## Routine release-build approval removed

JG authorized automatic build/signing on 2026-09-06. Commit `7ff4a39` adds master
push builds while retaining manual rebuilds, exact-source checkout, both CI
prerequisites and the environment-scoped signing key. Removed only the required
reviewer from `pip-release`; verified its branch policy still allows only the
`master` branch, not tags or PR branches. Automatic run 34015522341 passed both
verification jobs and entered signing without an approval stop. No automatic
host installation or merge was added, and the active canary stays pinned to
the already verified `21dc6da` release.

## 06:22 UTC: planner success, direct-worker permission failure, safe stop

The Astra planner completed task `t_94fd9479`. The controller accepted its result
exactly once and published the canonical inline plan as
[issue comment 5557419999](https://github.com/marmot-protocol/mdk/issues/1228#issuecomment-5557419999)
at 06:20:12 UTC. Ledger case #1228 is READY_TO_BUILD, state revision 3,
plan version 1; there is one accepted run and no PR.

The planner reports five focused Python tests passing and explicitly records
that full Rust/CI/live integration tests were not run. Its issue snapshot and
managed scratch/result handoff worked; this is not a full-pipeline success.

Between 06:20 and 06:22, direct attempts 1-3 failed with the same bounded result:

```text
direct worker runtime unavailable: failed to spawn bounded process:
Permission denied (os error 13)
```

Read-only inspection confirmed the case worktree is controller-owned mode 0700:
`/var/lib/pip/worktrees/mdk/repo-1055628515-issue-1228-workflow-3`.
The `pip-worker` identity can execute `/usr/local/bin/cursor-agent` but cannot
search/enter that worktree. `CursorHealthProbe` receives the worktree as its
working directory before creating attempt artifacts or executing the builder.
These are startup failures before builder inference, not a model rejection.
The correct fix must cover the worker's source/Git-metadata access contract,
not blindly relax the private ledger or repository boundary.

At the 06:22 supervision check two attempts had failed and the third was queued;
by emergency quiescence it too had failed. Verified the active/inert policy
guards, ran the existing `pause-runtime`, and restored inert revision 6. All
execution timers and the worker gateway are disabled; controller/direct worker
MainPIDs are zero. The gateway's failed shutdown state is not a live process.
The monitor is paused. Conversation Hermes and webhook ingress remain active.

The ledger records three failed direct attempts, zero running attempts, and
retains the pending work for explicit recovery. No case/task was reset and no
label was removed/reapplied. The worktree is clean. Root-only snapshot/evidence:
`/home/jeff/.cache/pip-canary/1228-6b0a144/failure-20260906T0622/`.
Original queue envelopes, planner output, board runs and logs remain in place.
